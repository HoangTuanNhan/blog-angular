export class Todo {
    public id: number;
    public name: string;
    public decription: string;
    public fee: number;

    // constructor(id: number, name: string, decription: string, fee: number) {
    //     this.id = id;
    //     this.name =name;
    //     this.decription = decription;
    //     this.fee = fee;
    // }
}

